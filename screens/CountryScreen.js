import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CountryScreen = () => {
  return (
    <View>
      <Text>CountryScreen CountryScreen</Text>
    </View>
  );
};

export default CountryScreen;

const styles = StyleSheet.create({});
